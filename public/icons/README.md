# Contabox icons

Placeholder. Replace with real icons (16/32/48/128 px PNG) before AMO submission.

For dev, you can use any 48px PNG. The build won't fail without these (manifest references them) but Firefox will log a warning.
